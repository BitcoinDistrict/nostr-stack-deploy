server {
    # Reference only. The runtime vhost is generated by scripts/deploy.sh
    # using DASHBOARD_DOMAIN and Certbot (HTTP → HTTPS redirect plus 443 with LE certs).
    listen 80;
    server_name DASHBOARD_DOMAIN_PLACEHOLDER;

    root /var/www/relay-dashboard;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}


