server {
    listen 80;
    server_name dashboard.relay.bitcoindistrict.org;

    root /var/www/relay-dashboard;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}


